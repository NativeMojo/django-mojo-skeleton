#!/usr/bin/env bash
# Convenience wrapper — starts the local ASGI dev server.
# All arguments are forwarded to asgi_local.
#
# Usage:
#   ./bin/run_dev_server
#   ./bin/run_dev_server --log-level debug

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/asgi_local" "$@"
